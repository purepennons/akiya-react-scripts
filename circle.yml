machine:
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
    CI: "true"
  node:
    version: 8.2.0

dependencies:
  override:
    - yarn
    - yarn lerna:install
  cache_directories:
    - ~/.cache/yarn
    
test:
  override:
    - yarn test