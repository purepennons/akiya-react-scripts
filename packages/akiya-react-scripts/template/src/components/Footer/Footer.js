import React from 'react'
import classnames from 'classnames/bind'

import styles from './Footer.scss'

const cx = classnames.bind(styles)

const Footer = props => <div className={cx('footer')}>Footer</div>

export default Footer
